@model int
@* perduodu int modeli *@
@{
    ViewData["Title"] = "Create";
}

<h1>Compose your comment</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="Text" class="control-label"></label>
                <input name="Text" class="form-control" />
                <span @*asp-validation-for="Text"*@ class="text-danger"></span>
                <input name="postId" type="hidden" asp-for="@Model" />
                @* name taiko i properties kaip Model. *@
            </div>
            <div class="form-group">
                <input type="submit" value="Comment" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
    <a asp-controller="Post" asp-action="Details" asp-route-id="PostId">Back to Image</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
